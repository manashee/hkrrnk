version: '2'
services:
    todo-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/todo/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=todo
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
